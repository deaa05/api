[project]
name = "main"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
   "fastapi[standard]==0.138.2",
   "supabase",
   "python-dotenv",
   "requests"
]

[tool.fastapi]
entrypoint = "main:app"